services:
  signal-api:
    # Pinned: all protocol behavior (incl. no-self-echo, receipt shapes) is
    # version-dependent — upgrade only after re-running the M1 protocol checks.
    image: bbernhard/signal-cli-rest-api:0.99
    container_name: signal-api
    restart: always
    ports:
      - "5000:8080"
    environment:
      - MODE=json-rpc
      - LOG_LEVEL=info
    volumes:
      - ${HOME}/.local/share/signal-api:/home/.local/share/signal-cli

  bridge:
    build: .
    container_name: signal-bridge
    restart: always
    ports:
      - "9099:9099"
    environment:
      - SIGNAL_API_URL=http://signal-api:8080
      - SIGNAL_NUMBER=${SIGNAL_NUMBER}   # set in your shell or a .env file
    volumes:
      - bridge-data:/data
    depends_on:
      - signal-api

volumes:
  bridge-data:
